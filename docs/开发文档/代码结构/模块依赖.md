# 模块依赖

> 阅读时机: 了解模块间依赖

---

## 依赖关系

```
┌─────────────────────────────────────────┐
│              表现层                      │
│  HomeView → WorkspaceView → EntityEditor│
│                    ↓                     │
├─────────────────────────────────────────┤
│              状态层                      │
│  ProjectProvider → EditorProvider       │
│                    ↓                     │
├─────────────────────────────────────────┤
│              服务层                      │
│  ProjectService → ModelingService       │
│                    ↓                     │
├─────────────────────────────────────────┤
│              数据层                      │
│  FileService → DataTypeService          │
└─────────────────────────────────────────┘
```

---

## 依赖列表

### Widget → Provider

| Widget | Provider |
|--------|----------|
| HomeView | ProjectProvider |
| EntityEditor | EditorProvider |
| ERDiagram | EditorProvider |

### Service → Service

| Service | 依赖 |
|---------|------|
| ProjectService | FileService |
| ModelingService | DataTypeService |
| CodeGenService | TemplateService, DataTypeService |

---

## 共享模块

### shared/models

所有模块依赖: `project.dart`, `entity.dart`, `field.dart`

### shared/utils

所有模块依赖: `uuid.dart`, `validators.dart`

---

## 相关文档

- [目录结构](目录结构.md)
- [系统架构](../../需求与架构/系统架构/系统架构.md)